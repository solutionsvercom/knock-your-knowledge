# Built frontend (Vite outputs here; Hostinger rebuilds on git push)
backend/public/

#env
.env
.env.*
!.env.example
!**/.env.example

# Cashfree 2FA public key (never commit the .pem)
*.pem
backend/keys/*.pem
!backend/keys/.gitkeep

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# Build output (never commit; production hosts run `npm run build` themselves)
dist
dist-ssr
build
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
